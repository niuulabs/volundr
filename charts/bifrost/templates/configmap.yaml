apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bifrost.fullname" . }}
  labels:
    {{- include "bifrost.labels" . | nindent 4 }}
data:
  bifrost.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
