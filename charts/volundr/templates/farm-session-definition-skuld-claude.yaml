{{- if and .Values.sessionDefinitions.skuldClaude.enabled (contains "FarmPodManager" .Values.podManager.adapter) }}
# Copyright (c) 2024-2025, NVIDIA CORPORATION.  All rights reserved.
#
# NVIDIA CORPORATION and its licensors retain all intellectual property
# and proprietary rights in and to this software, related documentation
# and any modifications thereto.  Any use, reproduction, disclosure or
# distribution of this software and related documentation without an express
# license agreement from NVIDIA CORPORATION is strictly prohibited.

# FarmSessionDefinition for Skuld-Claude (Claude Code session with code-server IDE)
# This defines the session type that Farm agents can use to create Claude sessions.
# Uses the shared skuld Helm chart, parameterised for Claude Code.

apiVersion: farm.nvidia.com/v1
kind: FarmSessionDefinition
metadata:
  name: skuld-claude
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "volundr.labels" . | nindent 4 }}
spec:
  name: skuld-claude

  # Labels for agent routing - only agents with matching labels will load this
  labels:
    {{- toYaml .Values.sessionDefinitions.skuldClaude.labels | nindent 4 }}

  active: {{ .Values.sessionDefinitions.skuldClaude.active }}

  # Helm chart configuration
  helm:
    chart: {{ .Values.sessionDefinitions.skuldClaude.helm.chart }}
    {{- with .Values.sessionDefinitions.skuldClaude.helm.repo }}
    repo: {{ . | quote }}
    {{- end }}
    {{- with .Values.sessionDefinitions.skuldClaude.helm.repoName }}
    repoName: {{ . | quote }}
    {{- end }}
    {{- with .Values.sessionDefinitions.skuldClaude.helm.version }}
    version: {{ . | quote }}
    {{- end }}

    # Default values (can be overridden by task_args at submission)
    values:
      session:
        model: {{ .Values.sessionDefinitions.skuldClaude.defaults.session.model | quote }}

      broker:
        cliType: {{ .Values.sessionDefinitions.skuldClaude.defaults.broker.cliType | default "claude" | quote }}
        transport: {{ .Values.sessionDefinitions.skuldClaude.defaults.broker.transport | default "sdk" | quote }}
        skipPermissions: {{ .Values.sessionDefinitions.skuldClaude.defaults.broker.skipPermissions | default true }}
        agentTeams: {{ .Values.sessionDefinitions.skuldClaude.defaults.broker.agentTeams | default false }}

      image:
        repository: {{ .Values.sessionDefinitions.skuldClaude.defaults.image.repository }}
        tag: {{ .Values.sessionDefinitions.skuldClaude.defaults.image.tag | quote }}

      resources:
        {{- toYaml .Values.sessionDefinitions.skuldClaude.defaults.resources | nindent 8 }}

      codeServer:
        enabled: {{ .Values.sessionDefinitions.skuldClaude.defaults.codeServer.enabled }}
        image:
          repository: {{ .Values.sessionDefinitions.skuldClaude.defaults.codeServer.image.repository }}
          tag: {{ .Values.sessionDefinitions.skuldClaude.defaults.codeServer.image.tag | quote }}
        resources:
          {{- toYaml .Values.sessionDefinitions.skuldClaude.defaults.codeServer.resources | nindent 10 }}

      {{- with .Values.sessionDefinitions.skuldClaude.defaults.imagePullSecrets }}
      global:
        imagePullSecrets:
          {{- toYaml . | nindent 10 }}
      {{- end }}

      ingress:
        enabled: true
        className: {{ .Values.sessionDefinitions.skuldClaude.defaults.ingress.className }}
        annotations:
          {{- toYaml .Values.sessionDefinitions.skuldClaude.defaults.ingress.annotations | nindent 10 }}
        tls:
          enabled: {{ .Values.sessionDefinitions.skuldClaude.defaults.ingress.tls.enabled }}
          {{- with .Values.sessionDefinitions.skuldClaude.defaults.ingress.tls.secretName }}
          secretName: {{ . | quote }}
          {{- end }}

      {{- with .Values.sessionDefinitions.skuldClaude.defaults.git }}
      git:
        {{- with .credentials }}
        credentials:
          {{- with .secretName }}
          secretName: {{ . | quote }}
          {{- end }}
        {{- end }}
      {{- end }}

      localServices:
        {{- toYaml .Values.sessionDefinitions.skuldClaude.defaults.localServices | nindent 8 }}

      persistence:
        enabled: true
        existingClaim: {{ include "volundr.sessionsPvcName" . }}
        mountPath: {{ .Values.sessionDefinitions.skuldClaude.defaults.persistence.mountPath | quote }}

      {{- if .Values.storage.home.enabled }}
      homeVolume:
        enabled: true
        existingClaim: {{ include "volundr.homePvcName" . }}
        mountPath: {{ .Values.sessionDefinitions.skuldClaude.defaults.homeVolume.mountPath | default "/volundr/home" | quote }}
        credentialFiles:
          secretName: {{ .Values.sessionDefinitions.skuldClaude.defaults.homeVolume.credentialFiles.secretName | quote }}
          destDir: {{ .Values.sessionDefinitions.skuldClaude.defaults.homeVolume.credentialFiles.destDir | default ".claude" | quote }}
          secretMountPath: {{ .Values.sessionDefinitions.skuldClaude.defaults.homeVolume.credentialFiles.secretMountPath | default "/volundr/secrets/credential-files" | quote }}
      {{- end }}

      envSecrets:
        {{- toYaml .Values.sessionDefinitions.skuldClaude.defaults.envSecrets | nindent 8 }}

      {{- with .Values.sessionDefinitions.skuldClaude.defaults.envVars }}
      envVars:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.sessionDefinitions.skuldClaude.defaults.runtimeClassName }}
      runtimeClassName: {{ . | quote }}
      {{- end }}

      securityContext:
        {{- toYaml .Values.sessionDefinitions.skuldClaude.defaults.securityContext | nindent 8 }}

      # Volundr API URL for token usage reporting (resolved at template time)
      # When Envoy is enabled, use the internal service to bypass JWT validation
      volundr:
        apiUrl: "http://{{ include "volundr.fullname" . }}{{ if .Values.envoy.enabled }}-internal{{ end }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}"
{{- end }}
